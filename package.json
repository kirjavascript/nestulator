{
    "scripts": {
        "watch": "esbuild src/main.ts --bundle --watch --outfile=static/bundle.js --loader:.nes=binary",
        "build": "esbuild src/main.ts --bundle --minify --outfile=static/bundle.js --loader:.nes=binary"
    },
    "devDependencies": {
        "6502.ts": "^1.0.4",
        "esbuild": "^0.12.19"
    },
    "dependencies": { }
}
